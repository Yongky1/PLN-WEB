# Workflow Rules

- **Branch Management**: Never push directly to the `develop` branch. The `develop` branch is reserved for final/fixed code. Always work on and push to feature branches (like `farih`, `yongky`, etc.), and use proper Git workflows (like Pull Requests or controlled merging) to update `develop` in order to keep it stable and clean.
