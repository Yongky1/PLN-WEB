<nav id="main-navbar" class="fixed w-full z-50 bg-black/80 backdrop-blur-md border-b border-white/10 transition-transform duration-500">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-20">
      
      <!-- Brand Logo -->
      <div class="flex-shrink-0">
        <a href="/" class="flex items-center gap-3">
          <!-- Stand-in PLN Logo -->
          <div class="w-10 h-10 bg-[var(--color-pln-yellow)] flex items-center justify-center font-bold text-black brand-font text-xl rounded">
            PLN
          </div>
          <span class="font-bold text-xl tracking-wider text-white brand-font">PUSDIKLAT</span>
        </a>
      </div>
      
      <!-- Desktop Menu -->
      <div class="hidden md:block">
        <div class="ml-10 flex items-baseline space-x-8">
          <a href="/" class="<%= typeof currentPage !== 'undefined' && currentPage === 'home' ? 'text-[var(--color-pln-yellow)] border-b-2 border-[var(--color-pln-yellow)]' : 'text-gray-300 hover:text-white' %> px-3 py-2 text-sm font-bold tracking-widest uppercase transition-colors">Home</a>
          <a href="/tools" class="<%= typeof currentPage !== 'undefined' && currentPage === 'tools' ? 'text-[var(--color-pln-yellow)] border-b-2 border-[var(--color-pln-yellow)]' : 'text-gray-300 hover:text-white' %> px-3 py-2 text-sm font-bold tracking-widest uppercase transition-colors">Alat</a>
          <a href="/ModulKonstruksi" class="<%= typeof currentPage !== 'undefined' && currentPage === 'konstruksi' ? 'text-[var(--color-pln-yellow)] border-b-2 border-[var(--color-pln-yellow)]' : 'text-gray-300 hover:text-white' %> px-3 py-2 text-sm font-bold tracking-widest uppercase transition-colors">Konstruksi</a>
          <a href="/material" class="<%= typeof currentPage !== 'undefined' && currentPage === 'material' ? 'text-[var(--color-pln-yellow)] border-b-2 border-[var(--color-pln-yellow)]' : 'text-gray-300 hover:text-white' %> px-3 py-2 text-sm font-bold tracking-widest uppercase transition-colors">Material</a>
        </div>
      </div>
      
      <!-- CTA / Contact -->
      <div class="hidden md:block">
        <a href="#contact" class="bg-white/5 border border-white/10 hover:border-white text-white px-6 py-2 rounded text-xs font-bold tracking-widest uppercase transition-all duration-300 hover:bg-white hover:text-black shadow-[0_0_15px_rgba(255,255,255,0.05)]">Contact Us</a>
      </div>
      
      <!-- Mobile menu button -->
      <div class="-mr-2 flex md:hidden">
        <button type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-white/10 focus:outline-none" aria-controls="mobile-menu" aria-expanded="false" onclick="document.getElementById('mobile-menu').classList.toggle('hidden')">
          <span class="sr-only">Open main menu</span>
          <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div class="md:hidden hidden bg-black/95 backdrop-blur-lg border-b border-white/10" id="mobile-menu">
    <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
      <a href="/" class="text-[var(--color-pln-yellow)] block px-3 py-2 rounded-md text-base font-medium">Home</a>
      <a href="#explore" class="text-gray-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">Alat</a>
      <a href="#explore" class="text-gray-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">Konstruksi</a>
      <a href="#explore" class="text-gray-300 hover:text-white block px-3 py-2 rounded-md text-base font-medium">Material</a>
    </div>
  </div>
</nav>

<!-- Auto-hide Navbar Script -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const navbar = document.getElementById("main-navbar");
    let lastScrollY = window.scrollY;
    // Throttle ticking variable
    let ticking = false;
    
    window.addEventListener("scroll", () => {
      if (!ticking) {
        window.requestAnimationFrame(() => {
          if (window.scrollY > 80) {
            if (window.scrollY > lastScrollY) {
              // Scrolling down
              navbar.classList.add("-translate-y-full");
            } else {
              // Scrolling up
              navbar.classList.remove("-translate-y-full");
            }
          } else {
            // Right at the top
            navbar.classList.remove("-translate-y-full");
          }
          lastScrollY = window.scrollY;
          ticking = false;
        });
        ticking = true;
      }
    }, { passive: true });
  });
</script>
